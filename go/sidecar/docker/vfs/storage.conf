[storage]
driver = "vfs"

[storage.options]
# When extracting image layers rootless Podman cannot lchown files to UIDs/GIDs
# outside its user-namespace mapping (there is no /etc/subuid range). Setting
# ignore_chown_errors skips those chown calls; files land owned by UID 1337.
ignore_chown_errors = "true"
